export enum NodeEnv {
  Local = 'local',
  Develop = 'develop',
  Product = 'product',
}

export enum MetadataKey {
  RolePolicy = 'role-policy',
}

export enum DateTimeFormat {
  YYYY_MM_DD = 'yyyy-MM-dd',
}
